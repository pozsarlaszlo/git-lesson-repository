package com.examples.functions;

import com.example.ownProjects.SudokuSolver;

public class Greet {
    public static void main(String[] args) {
        String al = "Green Fox";
        greet(al);
    }

    public static String greet(String t1) {
        String t2 = ("Welcome " + t1 + "!");
        System.out.println(t2);
        return t2;
    }

    //  From now on, create the usual class wrapper
//  and main method on your own

// - Create a string variable named `al` and assign the value `Green Fox` to it
// - Create a function called `greet()` that greets its input parameter
//     - It prints a greeting message e.g. `Greetings dear Green Fox`
// - Greet `al`

}
